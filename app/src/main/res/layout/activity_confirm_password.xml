<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:background="@color/app_bg"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    xmlns:app="http://schemas.android.com/apk/res-auto"

    tools:context="cf.paradoxie.dizzypassword.activity.ConfirmPassword">
    <include
        android:id="@+id/backview"
        layout="@layout/back_layout"></include>
    <TextView
        android:id="@+id/info"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:text="请再次输入以确认"
        android:layout_marginTop="30dp"
        android:layout_below="@id/backview"
        android:textColor="@android:color/black"
        android:textSize="18sp"
        />
    <cf.paradoxie.dizzypassword.password.PswInputView
        android:layout_width="match_parent"
        app:count="8"
        android:layout_margin="15dp"
        android:layout_below="@id/info"
        android:id="@+id/psw_input"
        android:background="@color/white"
        android:layout_height="wrap_content" />
    <Button
        android:layout_width="match_parent"
        android:text="确认"
        android:id="@+id/ok"
        android:layout_marginTop="4dp"
        android:layout_marginLeft="15dp"
        android:layout_marginRight="15dp"
        android:layout_below="@id/psw_input"
        android:background="@drawable/click_bg"
        android:layout_height="wrap_content" />

</RelativeLayout>
